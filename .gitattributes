# Golden files are compared byte for byte, so they have to arrive with LF
# on every platform: a Windows checkout that rewrote them as CRLF would
# fail the renderer's golden tests for a reason nobody would look for.
* text=auto eol=lf
*.png binary
*.pdf binary
